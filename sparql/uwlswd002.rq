# Querying across partitions

PREFIX edm: <http://www.europeana.eu/schemas/edm/>
PREFIX dct: <http://purl.org/dc/terms/>

SELECT ?srTitle ?cdmUrl

WHERE {

?agg edm:aggregatedCHO ?sr ;
	edm:isShownAt ?cdmUrl .
	
?sr dct:title ?srTitle .

FILTER regex(?srTitle, "washington", "i")

}